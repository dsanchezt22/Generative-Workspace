fastapi>=0.111.0
uvicorn[standard]>=0.29.0
google-genai>=0.8.0
python-dotenv>=1.0.0
pydantic>=2.0.0
# Screenshot capture engine: image downscale/normalize before the vision call.
# Lazy-imported (src/services/capture/capture.py) — the engine degrades gracefully
# if it's absent, so it stays optional for a minimal install.
Pillow>=10.0.0
# R-211 document grounding: server-side PDF text extraction so non-multimodal
# providers can still ground on uploaded documents. Lazy-imported (src/services/extract.py).
pypdf>=4.0.0
# itsdangerous = SessionMiddleware's signed session cookies (src/main.py) — a
# runtime dependency, not a test dep (was previously mislabeled under "Dev / test").
itsdangerous>=2.0.0
# python-multipart = FastAPI's multipart/form-data parsing for file uploads
# (studio import/capture, generate_from_file) — also runtime, not test-only.
python-multipart
