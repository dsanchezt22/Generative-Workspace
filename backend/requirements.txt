fastapi>=0.111.0
uvicorn[standard]>=0.29.0
google-genai>=0.8.0
python-dotenv>=1.0.0
pydantic>=2.0.0
# Screenshot capture engine: image downscale/normalize before the vision call.
# Lazy-imported (src/services/capture/capture.py) — the engine degrades gracefully
# if it's absent, so it stays optional for a minimal install.
Pillow>=10.0.0
# R-211 document grounding: server-side PDF text extraction so non-multimodal
# providers can still ground on uploaded documents. Lazy-imported (src/services/extract.py).
pypdf>=4.0.0

# Dev / test
pytest>=8.0.0
pytest-cov>=5.0.0
httpx>=0.27.0
pytest-asyncio>=0.23.0
itsdangerous>=2.0.0
python-multipart
